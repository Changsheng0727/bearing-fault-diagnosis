# Local Artifacts Directory

Generated files are written here during local experimentation and are intentionally ignored by Git.

Typical contents:

- processed `.npy` datasets
- trained `.h5` model weights
- Random Forest `.joblib` checkpoints
- TensorBoard logs

If you want to keep a small number of representative outputs in the repository, copy lightweight images into `assets/figures/` instead of committing the full artifacts.
