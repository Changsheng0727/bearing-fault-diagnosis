# Local Data Directory

Place raw datasets and any private reference materials here.

This folder is intentionally ignored by Git so the repository stays lightweight and safe to publish.

Expected local contents can include:

- CWRU bearing fault `.mat` files
- original project archives
- private source documents

The notebooks assume the raw bearing data remains available under this directory.
